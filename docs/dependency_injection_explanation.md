# Как работает Dependency Injection в FastAPI: магия `Depends(get_db_connection)`

## Код, который вызывает вопросы

```python
async def create_task(
    task: TaskCreate,
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
) -> TaskResponse:
    # В теле функции мы просто используем conn
    row = await conn.fetchrow("SELECT ...")
    # Но где вызов get_db_connection()? Его нет!
```

---

## Что такое Dependency Injection (DI)?

**Dependency Injection (Внедрение зависимостей)** - это паттерн, при котором зависимости (объекты, которые нужны функции) **автоматически передаются** в функцию, а не создаются внутри нее.

### Без DI (плохо):

```python
async def create_task(task: TaskCreate):
    # Создаем соединение ВНУТРИ функции
    conn = await asyncpg.connect(DATABASE_URL)  # ❌ Плохо!
    row = await conn.fetchrow("SELECT ...")
    await conn.close()
```

**Проблемы:**
- Каждая функция должна знать, как создать соединение
- Сложно тестировать (нельзя подменить соединение)
- Дублирование кода
- Сложно управлять жизненным циклом соединения

### С DI (хорошо):

```python
async def create_task(
    task: TaskCreate,
    conn: asyncpg.Connection,  # Соединение передается автоматически
):
    # Просто используем переданное соединение
    row = await conn.fetchrow("SELECT ...")
```

**Преимущества:**
- Функция не знает, как создается соединение
- Легко тестировать (можно подменить соединение)
- Нет дублирования кода
- FastAPI управляет жизненным циклом

---

## Как работает `Depends()` в FastAPI?

`Depends()` - это **декоратор/функция**, которая говорит FastAPI:
> "Эй, FastAPI! Когда вызываешь эту endpoint функцию, сначала вызови функцию `get_db_connection()`, получи результат, и передай его как параметр `conn`"

### Простыми словами:

```python
# Мы пишем:
async def create_task(
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
):
    pass

# FastAPI делает:
async def create_task(task: TaskCreate):
    # FastAPI автоматически вызывает:
    conn = await get_db_connection()  # ← FastAPI вызывает это за нас!
    # Теперь использует conn в нашей функции
    pass
```

---

## Что такое `Annotated`?

`Annotated[Type, Depends(...)]` - это способ **объединить** два типа информации:
1. **Тип данных** (`asyncpg.Connection`) - для type hints
2. **Метаданные** (`Depends(get_db_connection)`) - инструкция для FastAPI

### Старый способ (до Python 3.9):

```python
from typing import Annotated  # Нет в старых версиях

# Использовали просто Depends:
async def create_task(
    conn: asyncpg.Connection = Depends(get_db_connection),
):
    pass
```

### Новый способ (Python 3.9+):

```python
from typing import Annotated

async def create_task(
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
):
    pass
```

**Преимущества `Annotated`:**
- Более явный синтаксис
- Лучше для type checkers (mypy)
- Можно добавить несколько метаданных

---

## Как FastAPI обрабатывает `Depends()`?

### Шаг 1: FastAPI анализирует сигнатуру функции

```python
async def create_task(
    task: TaskCreate,
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
) -> TaskResponse:
    pass
```

FastAPI видит:
- `task: TaskCreate` - это тело запроса (Pydantic модель)
- `conn: Annotated[..., Depends(...)]` - это зависимость, нужно вызвать `get_db_connection()`
- `-> TaskResponse` - это тип ответа

### Шаг 2: FastAPI строит граф зависимостей

```
create_task()
  ├─ task: TaskCreate (из тела запроса)
  └─ conn: asyncpg.Connection
       └─ Зависит от: get_db_connection()
            └─ Использует: db_pool (глобальная переменная)
```

### Шаг 3: При запросе FastAPI выполняет:

```python
# Псевдокод того, что делает FastAPI:

async def handle_request(request):
    # 1. Парсит тело запроса
    task = TaskCreate.parse_obj(request.json)
    
    # 2. Вызывает зависимости (в правильном порядке)
    conn = await get_db_connection()  # ← FastAPI вызывает это!
    
    # 3. Вызывает endpoint функцию с готовыми параметрами
    result = await create_task(task=task, conn=conn)
    
    # 4. Возвращает результат
    return result
```

---

## Полный пример с пошаговым выполнением

### Код:

```python
# 1. Определяем dependency функцию
async def get_db_connection() -> asyncpg.Connection:
    if db_pool is None:
        raise HTTPException(...)
    async with db_pool.acquire() as connection:
        yield connection

# 2. Используем в endpoint
@app.post("/tasks")
async def create_task(
    task: TaskCreate,
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
) -> TaskResponse:
    row = await conn.fetchrow("INSERT ...")
    return TaskResponse(...)
```

### Что происходит при запросе `POST /tasks`:

```
1. Пользователь отправляет запрос:
   POST /tasks
   Body: {"title": "Task 1", ...}

2. FastAPI получает запрос:
   "Окей, нужно вызвать create_task()"

3. FastAPI анализирует параметры create_task():
   - task: TaskCreate → нужно распарсить из body
   - conn: Depends(get_db_connection) → нужно вызвать get_db_connection()

4. FastAPI вызывает зависимости:
   conn = await get_db_connection()
   ├─ Проверяет db_pool is None
   ├─ Берет соединение из пула: db_pool.acquire()
   └─ yield connection (пауза здесь)

5. FastAPI вызывает endpoint:
   result = await create_task(
       task=TaskCreate(title="Task 1", ...),
       conn=connection  # ← Передан автоматически!
   )
   ├─ Использует conn.fetchrow(...)
   └─ Возвращает TaskResponse

6. FastAPI завершает dependency:
   # Выход из async with в get_db_connection()
   # Соединение возвращается в пул

7. FastAPI возвращает ответ:
   HTTP 201 Created
   Body: {"id": 1, "title": "Task 1", ...}
```

---

## Почему `get_db_connection` использует `yield`?

Посмотри на код:

```python
async def get_db_connection() -> asyncpg.Connection:
    async with db_pool.acquire() as connection:
        yield connection  # ← Почему yield, а не return?
```

### С `return` (неправильно):

```python
async def get_db_connection() -> asyncpg.Connection:
    async with db_pool.acquire() as connection:
        return connection  # ❌ Проблема!
    # Соединение закроется СРАЗУ после return
    # async with не успеет вернуть соединение в пул
```

**Проблема:** `return` завершает функцию, `async with` блок завершается, соединение закрывается **до** того, как endpoint его использует.

### С `yield` (правильно):

```python
async def get_db_connection() -> asyncpg.Connection:
    async with db_pool.acquire() as connection:
        yield connection  # ✅ Правильно!
    # Соединение вернется в пул ТОЛЬКО после завершения endpoint
```

**Как работает:**
1. `yield connection` - паузирует выполнение, возвращает соединение
2. FastAPI получает соединение и передает в endpoint
3. Endpoint использует соединение
4. Endpoint завершается
5. FastAPI продолжает выполнение `get_db_connection()`
6. Выход из `async with` - соединение возвращается в пул

### Визуализация:

```
get_db_connection():
  ├─ async with db_pool.acquire() as connection:
  │    ├─ Берет соединение из пула
  │    ├─ yield connection  ← ПАУЗА! Соединение передано в endpoint
  │    │
  │    └─ [ENDPOINT РАБОТАЕТ ЗДЕСЬ]
  │       ├─ create_task() использует conn
  │       ├─ conn.fetchrow(...)
  │       └─ return TaskResponse
  │
  └─ Выход из async with  ← Соединение возвращается в пул
```

---

## Можно ли использовать `get_db_connection` явно?

Да, можно! Но это не рекомендуется:

```python
# ❌ Явный вызов (не рекомендуется):
@app.post("/tasks")
async def create_task(task: TaskCreate) -> TaskResponse:
    conn = await get_db_connection()  # Явный вызов
    row = await conn.fetchrow("INSERT ...")
    return TaskResponse(...)

# ✅ С Depends (рекомендуется):
@app.post("/tasks")
async def create_task(
    task: TaskCreate,
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
) -> TaskResponse:
    row = await conn.fetchrow("INSERT ...")
    return TaskResponse(...)
```

**Почему Depends лучше:**
- FastAPI управляет жизненным циклом
- Легче тестировать (можно подменить dependency)
- Нет дублирования кода
- Автоматическая обработка ошибок

---

## Как FastAPI определяет, что вызывать?

FastAPI использует **type hints** и **метаданные** для определения зависимостей:

```python
# FastAPI видит:
conn: Annotated[asyncpg.Connection, Depends(get_db_connection)]

# И понимает:
# 1. Тип параметра: asyncpg.Connection
# 2. Метаданные: Depends(get_db_connection)
# 3. Действие: вызвать get_db_connection() и передать результат
```

### Если нет `Depends()`:

```python
async def create_task(
    task: TaskCreate,
    conn: asyncpg.Connection,  # Нет Depends!
):
    pass
```

FastAPI не знает, откуда взять `conn`, и выдаст ошибку:
```
422 Unprocessable Entity: Field required: conn
```

---

## Пример с несколькими зависимостями

```python
# Dependency 1: получение соединения
async def get_db_connection() -> asyncpg.Connection:
    async with db_pool.acquire() as connection:
        yield connection

# Dependency 2: получение текущего пользователя
async def get_current_user(
    token: str = Header(...),
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
) -> User:
    # get_current_user тоже использует get_db_connection!
    user = await conn.fetchrow("SELECT * FROM users WHERE token = $1", token)
    return User(**user)

# Endpoint использует обе зависимости
@app.post("/tasks")
async def create_task(
    task: TaskCreate,
    conn: Annotated[asyncpg.Connection, Depends(get_db_connection)],
    user: Annotated[User, Depends(get_current_user)],
) -> TaskResponse:
    # И conn, и user доступны!
    row = await conn.fetchrow("INSERT ...", user.id)
    return TaskResponse(...)
```

**Граф зависимостей:**
```
create_task()
  ├─ conn: Depends(get_db_connection)
  └─ user: Depends(get_current_user)
       └─ conn: Depends(get_db_connection)  ← Та же зависимость!
```

FastAPI **кэширует** результат `get_db_connection()` для одного запроса, так что соединение не создается дважды!

---

## Полная визуализация работы

```
┌─────────────────────────────────────────────────────────┐
│  Пользователь: POST /tasks                              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  FastAPI: "Нужно вызвать create_task()"                 │
│                                                          │
│  Анализ параметров:                                     │
│  - task: TaskCreate → из body                            │
│  - conn: Depends(get_db_connection) → нужно вызвать      │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  FastAPI: "Вызываю зависимости"                          │
│                                                          │
│  await get_db_connection():                             │
│    ├─ if db_pool is None: raise HTTPException            │
│    ├─ async with db_pool.acquire() as connection:       │
│    │    └─ yield connection  ← ПАУЗА                     │
│    │                                                      │
│    └─ [СОЕДИНЕНИЕ ПЕРЕДАНО В ENDPOINT]                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  FastAPI: "Вызываю endpoint"                            │
│                                                          │
│  await create_task(                                     │
│      task=TaskCreate(...),                              │
│      conn=connection  ← Автоматически передан!          │
│  ):                                                     │
│    ├─ row = await conn.fetchrow("INSERT ...")           │
│    └─ return TaskResponse(...)                          │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  FastAPI: "Завершаю зависимости"                        │
│                                                          │
│  # Продолжение get_db_connection() после yield:         │
│  # Выход из async with                                   │
│  # Соединение возвращается в пул                        │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  FastAPI: "Возвращаю ответ"                             │
│                                                          │
│  HTTP 201 Created                                       │
│  Body: {"id": 1, "title": "Task 1", ...}                │
└─────────────────────────────────────────────────────────┘
```

---

## Резюме

1. **`Depends(get_db_connection)`** - говорит FastAPI вызвать `get_db_connection()` перед endpoint
2. **FastAPI автоматически** вызывает dependency и передает результат как параметр
3. **`yield` в dependency** - позволяет управлять жизненным циклом (соединение возвращается в пул после использования)
4. **`Annotated[Type, Depends(...)]`** - объединяет type hint и метаданные для FastAPI
5. **В теле endpoint** мы просто используем `conn`, не вызывая `get_db_connection()` явно

Это и есть **магия Dependency Injection** - зависимости передаются автоматически, без явных вызовов!

---

## Часто задаваемые вопросы

### Q: Можно ли использовать `get_db_connection()` без `Depends()`?

A: Да, но это не рекомендуется. FastAPI не будет управлять жизненным циклом соединения.

### Q: Что если `get_db_connection()` выбросит исключение?

A: FastAPI автоматически обработает его и вернет HTTP ошибку клиенту.

### Q: Можно ли использовать обычную функцию вместо async?

A: Да, но для работы с БД лучше использовать async функции.

### Q: Можно ли передать параметры в dependency?

A: Да! FastAPI автоматически извлекает параметры из запроса:

```python
async def get_db_connection(
    timeout: int = 60,  # FastAPI извлечет из query параметров
) -> asyncpg.Connection:
    # ...
```

---

Это и есть вся "магия" Dependency Injection в FastAPI! 🎩✨

