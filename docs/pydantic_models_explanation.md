# Как работают Pydantic модели в update функциях

## Концепция: Pydantic как мост между JSON и Python

Pydantic модели - это **валидация и преобразование** данных из JSON запроса в Python объекты с проверкой типов.

---

## Модель `TaskUpdate`

```python
class TaskUpdate(BaseModel):
    """Request model for updating a task."""

    title: Optional[str] = None
    description: Optional[str] = None
    status_id: Optional[int] = None
    assignee_id: Optional[int] = None
    deadline_start: Optional[date] = None
    deadline_end: Optional[date] = None
```

### Что означает `Optional[str] = None`?

- `Optional[str]` - поле может быть `str` или `None`
- `= None` - **дефолтное значение**, если поле не передано

---

## Как FastAPI использует Pydantic

### Шаг 1: FastAPI получает JSON запрос

```http
PUT /tasks/1
Content-Type: application/json

{
  "title": "New Title",
  "status_id": 2
}
```

### Шаг 2: FastAPI парсит JSON в Pydantic модель

FastAPI автоматически:
1. Парсит JSON
2. Валидирует типы
3. Создает объект `TaskUpdate`

```python
# FastAPI делает это автоматически:
task = TaskUpdate(
    title="New Title",      # Передано в JSON
    status_id=2,            # Передано в JSON
    description=None,       # Не передано → дефолт None
    assignee_id=None,       # Не передано → дефолт None
    deadline_start=None,    # Не передано → дефолт None
    deadline_end=None       # Не передано → дефолт None
)
```

### Шаг 3: Объект передается в endpoint

```python
async def update_task(
    task_id: int,
    task: TaskUpdate,  # ← Pydantic объект уже готов!
    conn: ...,
):
    # task.title = "New Title" (не None)
    # task.description = None (дефолт)
    # task.status_id = 2 (не None)
    # ...
```

---

## Разные сценарии: что происходит с полями?

### Сценарий 1: Поле передано

**JSON:**
```json
{
  "title": "New Title"
}
```

**Pydantic объект:**
```python
task = TaskUpdate(
    title="New Title",  # ✅ Передано, не None
    description=None,   # Не передано → None
    status_id=None,     # Не передано → None
    # ...
)
```

**В коде:**
```python
if task.title is not None:  # ✅ True - поле передано
    update_fields.append("title = $1")
    values.append(task.title)
```

---

### Сценарий 2: Поле не передано

**JSON:**
```json
{
  "title": "New Title"
}
# description не передано
```

**Pydantic объект:**
```python
task = TaskUpdate(
    title="New Title",
    description=None,  # Не передано → дефолт None
    # ...
)
```

**В коде:**
```python
if task.description is not None:  # ❌ False - поле None
    # Этот блок НЕ выполнится
    update_fields.append("description = $1")
```

---

### Сценарий 3: Поле явно передано как `null`

**JSON:**
```json
{
  "title": "New Title",
  "description": null  // Явно передано null
}
```

**Pydantic объект:**
```python
task = TaskUpdate(
    title="New Title",
    description=None,  # Явно передано null → None
    # ...
)
```

**В коде:**
```python
if task.description is not None:  # ❌ False - поле None
    # Этот блок НЕ выполнится
    # description НЕ обновится в БД
```

**Важно:** Если передать `null`, поле останется `None` и не обновится. Это правильное поведение для Partial Update!

---

### Сценарий 4: Все поля не переданы

**JSON:**
```json
{}
```

**Pydantic объект:**
```python
task = TaskUpdate(
    title=None,        # Все поля None
    description=None,
    status_id=None,
    # ...
)
```

**В коде:**
```python
# Валидация
if not any([
    task.title is not None,        # False
    task.description is not None,  # False
    # ... все False
]):
    raise HTTPException(400, "At least one field must be provided")
```

---

## Визуализация: JSON → Pydantic → SQL

```
┌─────────────────────────────────────────────────────────┐
│  Шаг 1: JSON запрос                                      │
│                                                          │
│  PUT /tasks/1                                            │
│  Body: {"title": "New", "status_id": 2}                 │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Шаг 2: FastAPI парсит JSON                             │
│                                                          │
│  FastAPI автоматически:                                 │
│  1. Парсит JSON                                          │
│  2. Валидирует типы                                      │
│  3. Создает TaskUpdate объект                           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Шаг 3: Pydantic объект                                  │
│                                                          │
│  task = TaskUpdate(                                      │
│      title="New",          # Передано                    │
│      status_id=2,          # Передано                    │
│      description=None,     # Не передано → дефолт       │
│      assignee_id=None,     # Не передано → дефолт       │
│      deadline_start=None,  # Не передано → дефолт       │
│      deadline_end=None     # Не передано → дефолт       │
│  )                                                       │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Шаг 4: Проверка полей в коде                           │
│                                                          │
│  if task.title is not None:        # ✅ True            │
│      update_fields.append("title = $1")                 │
│      values.append("New")                                │
│                                                          │
│  if task.description is not None:  # ❌ False           │
│      # Пропускаем                                        │
│                                                          │
│  if task.status_id is not None:    # ✅ True            │
│      update_fields.append("status_id = $2")             │
│      values.append(2)                                    │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Шаг 5: SQL запрос                                       │
│                                                          │
│  UPDATE tasks                                            │
│  SET title = $1, status_id = $2, updated_at = NOW()    │
│  WHERE id = $3                                           │
│                                                          │
│  # description НЕ обновляется!                           │
└─────────────────────────────────────────────────────────┘
```

---

## Важные моменты о Pydantic

### 1. Автоматическая валидация типов

**Если передать неправильный тип:**

```json
{
  "title": "New Title",
  "status_id": "not a number"  // ❌ Должно быть число
}
```

**Pydantic выбросит ошибку:**
```json
{
  "detail": [
    {
      "loc": ["body", "status_id"],
      "msg": "value is not a valid integer",
      "type": "type_error.integer"
    }
  ]
}
```

**FastAPI автоматически вернет 422 Unprocessable Entity** - даже до того, как код endpoint выполнится!

---

### 2. Дефолтные значения

```python
class TaskUpdate(BaseModel):
    title: Optional[str] = None  # Дефолт = None
```

**Если поле не передано:**
- Pydantic автоматически устанавливает `None`
- Не нужно проверять `hasattr()` или `getattr()`

**Пример:**
```python
# JSON: {"title": "New"}
task = TaskUpdate(title="New")
# task.description автоматически = None
# task.status_id автоматически = None
```

---

### 3. Разница: `None` vs не переданное поле

**В нашем коде:**
```python
if task.title is not None:  # Проверяем значение
    # Обновляем поле
```

**Pydantic не различает:**
- Поле не передано → `None` (дефолт)
- Поле передано как `null` → `None`

**Оба случая дают `None`!**

**Если нужно различать:**
```python
# Использовать exclude_unset
task_dict = task.dict(exclude_unset=True)
# exclude_unset=True → только переданные поля

if "title" in task_dict:  # Поле было передано
    update_fields.append("title = $1")
```

**Но в нашем случае это не нужно** - мы хотим обновлять только не-None поля, и это работает правильно!

---

## Примеры разных запросов

### Пример 1: Одно поле

**JSON:**
```json
{
  "title": "Updated Title"
}
```

**Pydantic:**
```python
TaskUpdate(
    title="Updated Title",  # ✅ Не None
    description=None,      # Не передано
    status_id=None,        # Не передано
    # ...
)
```

**SQL:**
```sql
UPDATE tasks
SET title = $1, updated_at = NOW()
WHERE id = $2
```

---

### Пример 2: Несколько полей

**JSON:**
```json
{
  "title": "New Title",
  "status_id": 3,
  "assignee_id": 5
}
```

**Pydantic:**
```python
TaskUpdate(
    title="New Title",     # ✅ Не None
    status_id=3,           # ✅ Не None
    assignee_id=5,         # ✅ Не None
    description=None,     # Не передано
    # ...
)
```

**SQL:**
```sql
UPDATE tasks
SET title = $1, status_id = $2, assignee_id = $3, updated_at = NOW()
WHERE id = $4
```

---

### Пример 3: Пустой объект

**JSON:**
```json
{}
```

**Pydantic:**
```python
TaskUpdate(
    title=None,           # Все None
    description=None,
    status_id=None,
    # ...
)
```

**Валидация:**
```python
if not any([...]):  # Все False
    raise HTTPException(400, "At least one field must be provided")
```

**Результат:** HTTP 400 Bad Request

---

### Пример 4: Явный null

**JSON:**
```json
{
  "title": "New Title",
  "description": null
}
```

**Pydantic:**
```python
TaskUpdate(
    title="New Title",    # ✅ Не None
    description=None,      # Явно null → None
    status_id=None,        # Не передано
    # ...
)
```

**SQL:**
```sql
UPDATE tasks
SET title = $1, updated_at = NOW()
WHERE id = $2
-- description НЕ обновляется (остается прежним)
```

---

## Как Pydantic обрабатывает типы

### Строки

```python
title: Optional[str] = None
```

**Валидно:**
- `"Hello"` → `str`
- `null` → `None`
- Не передано → `None` (дефолт)

**Невалидно:**
- `123` → Ошибка: "str type expected"
- `true` → Ошибка: "str type expected"

---

### Числа

```python
status_id: Optional[int] = None
```

**Валидно:**
- `1` → `int`
- `0` → `int`
- `null` → `None`
- Не передано → `None`

**Невалидно:**
- `"1"` → Ошибка: "int type expected"
- `1.5` → Ошибка: "int type expected"

---

### Даты

```python
deadline_start: Optional[date] = None
```

**Валидно:**
- `"2024-01-15"` → `date(2024, 1, 15)`
- `null` → `None`
- Не передано → `None`

**Невалидно:**
- `"not a date"` → Ошибка: "date format invalid"
- `"2024-13-45"` → Ошибка: "date format invalid"

---

## Сравнение: TaskCreate vs TaskUpdate

### TaskCreate (все поля обязательны, кроме Optional)

```python
class TaskCreate(BaseModel):
    title: str                    # Обязательно!
    description: Optional[str] = None  # Опционально
    status_id: int                # Обязательно!
    creator_id: int               # Обязательно!
    # ...
```

**Если не передать обязательное поле:**
```json
{
  "title": "New Task"
  // status_id не передано
}
```

**Pydantic выбросит ошибку:**
```json
{
  "detail": [
    {
      "loc": ["body", "status_id"],
      "msg": "field required"
    }
  ]
}
```

---

### TaskUpdate (все поля опциональны)

```python
class TaskUpdate(BaseModel):
    title: Optional[str] = None      # Опционально
    description: Optional[str] = None # Опционально
    status_id: Optional[int] = None  # Опционально
    # ...
```

**Можно передать любое подмножество полей:**
```json
{
  "title": "New Title"
  // Остальные поля не обязательны
}
```

**Pydantic создаст объект:**
```python
TaskUpdate(
    title="New Title",
    description=None,  # Дефолт
    status_id=None,    # Дефолт
    # ...
)
```

---

## Полный поток данных

```
1. Клиент отправляет запрос:
   PUT /tasks/1
   {"title": "New", "status_id": 2}

2. FastAPI получает запрос:
   - Парсит JSON
   - Определяет, что нужна модель TaskUpdate

3. Pydantic валидирует:
   - title: "New" → str ✅
   - status_id: 2 → int ✅
   - description: не передано → None (дефолт)
   - Создает объект TaskUpdate

4. FastAPI передает в endpoint:
   async def update_task(
       task_id: int,           # Из path: 1
       task: TaskUpdate,       # Pydantic объект
       conn: ...,
   ):

5. Код проверяет поля:
   if task.title is not None:        # ✅ True
       update_fields.append("title = $1")
       values.append("New")

   if task.status_id is not None:    # ✅ True
       update_fields.append("status_id = $2")
       values.append(2)

   if task.description is not None: # ❌ False
       # Пропускаем

6. SQL запрос:
   UPDATE tasks
   SET title = $1, status_id = $2, updated_at = NOW()
   WHERE id = $3

7. Результат:
   - title обновлен
   - status_id обновлен
   - description остался прежним ✅
```

---

## Резюме

### Как Pydantic работает:

1. **Парсинг JSON** - FastAPI автоматически парсит JSON в Pydantic модель
2. **Валидация типов** - Pydantic проверяет, что типы правильные
3. **Дефолтные значения** - Если поле не передано, устанавливается `None`
4. **Создание объекта** - Pydantic создает Python объект с валидными данными
5. **Передача в endpoint** - Объект передается как параметр функции

### Ключевые моменты:

- `Optional[str] = None` - поле может быть `str` или `None`, дефолт `None`
- Если поле не передано → автоматически `None`
- Если поле передано как `null` → `None`
- Проверка `if task.title is not None` - определяет, было ли поле передано (и не null)

### Преимущества Pydantic:

- ✅ Автоматическая валидация типов
- ✅ Автоматическая обработка дефолтных значений
- ✅ Защита от невалидных данных
- ✅ Четкие сообщения об ошибках
- ✅ Type hints для IDE

Pydantic - это мост между JSON запросом и Python кодом, который гарантирует, что данные валидны и правильно типизированы! 🎯

